class IconDownloader(object):
    """
    After the AA is built, we will download all of its icons into a directory, if they are not already there
    Then, sprited icons will be built into a new directory with accompanying CSS
    """
    
    def get_icon(self, icon_id):
        pass